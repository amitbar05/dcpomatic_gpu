# auto_iter queue — Run 14. Combined V198-aware experiments.

# === Try MAX_BP reductions for fast mode (currently 12) =====================

# === Combined: V198 MAX_BP=13 + smaller fast MAX_BP =========================
script:sed -i 's|kernel_ebcot_t1<true, 12, __half>|kernel_ebcot_t1<true, 11, __half>|; s|fast_mode \? 0.5f : 1.0f|fast_mode ? 0.55f : 1.0f|' src/lib/cuda_j2k_encoder.cu
script:sed -i 's|kernel_ebcot_t1<true, 12, __half>|kernel_ebcot_t1<true, 11, __half>|; s|fast_mode \? 0.5f : 1.0f|fast_mode ? 0.45f : 1.0f|' src/lib/cuda_j2k_encoder.cu

# === bp_skip combinations for fast ======================================
sed:src/lib/cuda_j2k_encoder.cu:s|int  bp_skip    = fast_mode \? 1 : 0;|int  bp_skip    = fast_mode ? 0 : 0;|

# === T1 single-component launch alternative (would be invalid sed) ======
# Skip — too complex

# === Pair: MAX_BP=13 + bp_skip (correct) ================================
script:sed -i 's|int  bp_skip    = fast_mode \? 1 : 0;|int  bp_skip    = fast_mode ? 1 : 1;|' src/lib/cuda_j2k_encoder.cu

# === Try fast_step_mult fine ============================================
sed:src/lib/cuda_j2k_encoder.cu:s|fast_mode \? 3.0f : 1.0f|fast_mode ? 2.8f : 1.0f|
sed:src/lib/cuda_j2k_encoder.cu:s|fast_mode \? 3.0f : 1.0f|fast_mode ? 3.2f : 1.0f|

# === Combined fast tweaks ===============================================
script:sed -i 's|fast_mode \? 3.0f : 1.0f|fast_mode ? 2.9f : 1.0f|; s|fast_mode \? 0.5f : 1.0f|fast_mode ? 0.52f : 1.0f|; s|fast_mode \? 640 : CB_BUF_SIZE|fast_mode ? 656 : CB_BUF_SIZE|' src/lib/cuda_j2k_encoder.cu

# === Coordinated level weight (level-1 to level-3 only) =================
sed:src/lib/gpu_ebcot_t2.h:s|{1.20f, 1.12f, 1.05f, 0.95f, 0.85f, 0.65f, 0.65f, 0.65f}|{1.18f, 1.10f, 1.04f, 0.95f, 0.85f, 0.65f, 0.65f, 0.65f}|
sed:src/lib/gpu_ebcot_t2.h:s|{1.20f, 1.12f, 1.05f, 0.95f, 0.85f, 0.65f, 0.65f, 0.65f}|{1.22f, 1.14f, 1.06f, 0.95f, 0.85f, 0.65f, 0.65f, 0.65f}|

# === Final rebaseline noops ==================
noop: end of Run 14 queue 1
noop: end of Run 14 queue 2
