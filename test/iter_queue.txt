# auto_iter queue — Run 17. Push V199 boundaries.

# === Stage YY: aggressive high threshold (allow checker_64 to retry) ====
sed:src/lib/cuda_j2k_encoder.cu:s|adaptive_thresh_high = 0.55f;|adaptive_thresh_high = 0.80f;|
sed:src/lib/cuda_j2k_encoder.cu:s|adaptive_thresh_high = 0.55f;|adaptive_thresh_high = 0.95f;|
sed:src/lib/cuda_j2k_encoder.cu:s|adaptive_thresh_high = 0.55f;|adaptive_thresh_high = 0.70f;|

# === Stage YY: lower low threshold (allow single_impulse to retry) ====
# This might still regress impulse but try
sed:src/lib/cuda_j2k_encoder.cu:s|adaptive_thresh_low  = 0.005f;|adaptive_thresh_low  = 0.001f;|

# === Stage YY: combined max_attempts and threshold ====
script:sed -i 's|adaptive_max_attempts = fast_mode \? 1 : 2;|adaptive_max_attempts = fast_mode ? 1 : 3;|; s|adaptive_thresh_high = 0.55f;|adaptive_thresh_high = 0.30f;|' src/lib/cuda_j2k_encoder.cu
script:sed -i 's|adaptive_max_attempts = fast_mode \? 1 : 2;|adaptive_max_attempts = fast_mode ? 1 : 4;|; s|adaptive_thresh_high = 0.55f;|adaptive_thresh_high = 0.50f;|' src/lib/cuda_j2k_encoder.cu

# === Stage YY: V199 enabled for fast mode too ====
sed:src/lib/cuda_j2k_encoder.cu:s|adaptive_max_attempts = fast_mode \? 1 : 2;|adaptive_max_attempts = fast_mode ? 2 : 2;|

# === Stage ZZ: V199 with different halving ratio ====
# Halve currently. Try 0.7× (less aggressive) or 0.3× (more aggressive)
sed:src/lib/cuda_j2k_encoder.cu:s|current_step \*= 0.5f|current_step *= 0.7f|
sed:src/lib/cuda_j2k_encoder.cu:s|current_step \*= 0.5f|current_step *= 0.4f|

# === Stage ZZ: combined with V199 active ====
sed:src/lib/cuda_j2k_encoder.cu:s|fast_mode \? 640 : CB_BUF_SIZE|fast_mode ? 640 : 1280|
sed:src/lib/cuda_j2k_encoder.cu:s|fast_mode \? 640 : CB_BUF_SIZE|fast_mode ? 720 : CB_BUF_SIZE|

noop: end of Run 17 queue
